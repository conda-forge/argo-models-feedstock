{% set name = "argo-models" %}
{% set version = "2.6.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 3d38497b6b42c595e90f88ced22adc266a4d8a909c75fdfc2a3a824b75c825cd

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python
    - pip
  run:
    - python
    - six

test:
  imports:
    - argo
    - argo.models

about:
  home: https://github.com/e2fyi/argo-models
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Generated openapi models for Argo objects.
  description: |
    argo-models is a argo namespaced package with the argo.models subpackage
    which contains the generated OpenAPI models from Argo.

extra:
  recipe-maintainers:
    - nicolas-geniteau
    - maximerihouey
    - ncolomer
